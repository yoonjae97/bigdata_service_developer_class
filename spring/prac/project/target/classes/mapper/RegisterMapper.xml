<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.home.dao.RegisterDAO">
	<insert id="registerInsert">
		insert into Member(MemberId, MemberPwd, MemberAddr, 
		MemberEmail, MemberName, MemberTel, MemberBirth, MemberGender)
		values(#{MemberId}, #{MemberPwd}, #{MemberAddr}, #{MemberEmail}, #{MemberName},
		#{MemberTel}, #{MemberBirth}, #{MemberGender})
	</insert>
	
	<select id="loginOk" resultType="com.smart.home.dto.RegisterDTO">
		select memberid, membername from members
		where memberid=#{MemberId} and memberpwd=#{MemberPwd}
	</select>
	
	<select id="findId" resultType="com.smart.home.dto.RegisterDTO">
		select memberid from members
		where memberid=#{param1} and memberEmail=#{param2}
	</select>
	
	<select id="dupChk" resultType="String">
		select memberid from members
		where memberid=#{param1}
	</select>
	
	<select id="findPwd" resultType="String">
		select memberPwd from members
		where memberId=#{param1} and memberEmail=#{param2}
	</select>
</mapper>
